<UserControl x:Class="ParticleEditor.Views.ParameterTabs.BurstTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:helpers="clr-namespace:ParticleEditor.Helpers"
             xmlns:viewModels="clr-namespace:ParticleEditor.ViewModels"
             xmlns:parameterTabs="clr-namespace:ParticleEditor.ViewModels.ParameterTabs"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.DataContext>
            <parameterTabs:BurstTabViewModel/>
        </Grid.DataContext>
        
        <Grid.Resources>
            <helpers:StringToFloat x:Key="StringToFloat"></helpers:StringToFloat>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>
            <Label Content="Time" Grid.Column="0" FontWeight="Bold"/>
            <Label Content="Particles" Grid.Column="1" FontWeight="Bold"/>
        </Grid>
        <Grid Grid.Row="1">
            <ListBox ItemsSource="{Binding MainViewModel.ParticleSystem.Bursts.Collection}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="{Binding Key}" Grid.Column="0"/>
                            <Label Content="{Binding Value}" Grid.Column="1"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>
            <TextBox Name="Text_Key" Text="0" Grid.Column="0"/>
            <TextBox Name="Text_Value" Text="0" Grid.Column="1"/>
            <Button Content="+" Command="{Binding AddBurstCommand}" CommandParameter="{Binding ElementName=Text_Key, Path=Text}" Grid.Column="2"/>
        </Grid>
    </Grid>
</UserControl>
