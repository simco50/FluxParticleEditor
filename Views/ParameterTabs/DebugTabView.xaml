<UserControl x:Class="ParticleEditor.Views.ParameterTabs.DebugTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:ParticleEditor.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300" x:Name="UserControl">
    <Grid>
        <ItemsControl BorderThickness="0" ItemsSource="{Binding Source={x:Static helpers:DebugLog.Entries}}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" >
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding}" TextWrapping="WrapWithOverflow" VerticalAlignment="Center"/>
                        <Separator VerticalAlignment="Bottom" Margin="0"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
