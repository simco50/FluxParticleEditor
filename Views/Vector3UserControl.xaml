<UserControl x:Class="ParticleEditor.Views.Vector3UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="300" Name="UserControl">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBox Text="{Binding ElementName=UserControl, Path=X}" Grid.Column="0"/>
        <TextBox Text="{Binding ElementName=UserControl, Path=Y}" Grid.Column="1"/>
        <TextBox Text="{Binding ElementName=UserControl, Path=Z}" Grid.Column="2"/>
    </Grid>
</UserControl>
